overwrite: true
schema: "src/gqlSchema.json"
documents: "./src/**/!(*.d).{ts,tsx}"
generates:
  src/graphqlTypes.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
config:
  maybeValue: T
  typesPrefix: GQL
  inlineFragmentTypes: combine
