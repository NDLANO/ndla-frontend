overwrite: true
schema: "src/gqlSchema.json"
documents: "src/queries.ts"
generates:
  src/graphqlTypes.ts:
    plugins:
      - "typescript"
config:
  maybeValue: T | undefined
  namingConvention:
    typeNames: ./src/util/gqlNamingFunction.js
